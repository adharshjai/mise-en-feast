# Vercel installs this one for api/index.py, which imports backend/main.py.
# Everything backend/main.py imports has to be here: a missing dependency
# crashes the serverless function at import time, so every /api route answers
# 500 rather than the endpoint's own error.
fastapi>=0.115
uvicorn[standard]>=0.30
python-dotenv>=1.0
google-genai>=1.0
pydantic>=2.0
python-multipart>=0.0.9
