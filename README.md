# Pantry Pal

A pantry that fills itself from grocery receipts and tells you what to cook.

- `frontend/` — the static web app (landing, login, app). See [frontend/README.md](frontend/README.md) for how to run it, connect Supabase and deploy on Vercel (Root Directory = `frontend`).
